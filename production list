have parsers:

{formula}:
{scalar}:
{fn_call}:
{scalar_path}:
{tensor_path}:
{null}:
{bool}:
{string}:
{number}:
{unary}:
{binary}:
{mixed}:
{aggregate}:

obviated:

{starter}:
{integer}:
{nonneg}:
{float}:
{digits}:

need parsers:

{index}:
{key}:
{scalar_index}:
{scalar_key}:
{scalar_subpath}:
{tensor_subpath}:
