version
    id
    datetime
    content
    parent      (version.id)

document
    id
    owner       (user.id)
    filename
    latest      (version.id)
    archived

user
    id
    login
    passhash
    full_name
